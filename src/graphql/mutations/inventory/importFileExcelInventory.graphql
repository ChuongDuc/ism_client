mutation ImportFileExcelInventory($input: ImportFileExcelInventoryInput!) {
    importFileExcelInventory(input: $input) {
        id
        productName
        code
        weight
        unit
        quantity
        fileName
        createdAt
        updatedAt
    }
}
