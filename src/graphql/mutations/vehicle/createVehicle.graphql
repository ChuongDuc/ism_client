mutation CreateVehicle($input: CreateVehicleInput!) {
    createVehicle(input: $input) {
        id
        driver {
            id
            email
            userName
            avatarURL
        }
        typeVehicle
        weight
        licensePlates
        note
        createdAt
        updatedAt
    }
}
